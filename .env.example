GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
GOOGLE_CLIENT_EMAIL=service-account@project.iam.gserviceaccount.com
SPREADSHEET_ID=your_spreadsheet_id
PORT=3000

# Login with 10 Minute School (public client ID — safe in the browser)
VITE_TENMS_CLIENT_ID=tenms_xxxxxxxxxxxxxxxx
# Optional: pin the OAuth redirect URI. Defaults to the current origin.
# VITE_TENMS_REDIRECT_URI=https://your-app.vercel.app

# Local development only. Never set this in production — SSO is the front door.
# ALLOW_PASSWORD_LOGIN=true

# Google Drive folder that claim documents are uploaded into.
DRIVE_FOLDER_ID=your_drive_folder_id
# Largest single upload, in MB. Vercel caps a request body at ~4.5 MB.
# MAX_UPLOAD_MB=4
# Only needed when the folder lives in a personal My Drive rather than a Shared
# Drive: switch on domain-wide delegation and name the user to act as.
# GOOGLE_IMPERSONATE_SUBJECT=someone@10minuteschool.com
